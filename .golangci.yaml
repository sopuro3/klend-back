linters:
  enable-all: true
  disable:
    - deadcode
    - depguard
    - exhaustivestruct
    - golint
    - ifshort
    - interfacer
    - maligned
    - nosnakecase
    - scopelint
    - structcheck
    - varcheck
    - godot
    - wrapcheck # return c.JSON()でエラーがでるので
